"use client";
export default function SearchResults(books: string[]) {
    return (
        <div>
            {/* {books.map((book) => (
                <li key={book}>book</li>
            ))} */}
            books.length
        </div>
    );
}
